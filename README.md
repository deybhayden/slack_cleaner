# slack_cleaner scripts

Script to clean up Slack channels by removing messages.

## Install

To install the required packages, run:

```bash
uv sync
```

## Usage

To run the script, use the following command:

```bash
uv run python cleanup.py
```
